import React from 'react'
import { StyleSheet } from 'react-native'
import { FavoriteSong, HomeScreen, Playlist, SearchSongs, Song, Splash } from './pages'

const App = () => {
  return (
      <FavoriteSong/>
  );
}

export default App

const styles = StyleSheet.create({})
